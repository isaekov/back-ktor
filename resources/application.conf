ktor {
    deployment {
        port = 9999
    }
    application {
        modules = [ ru.hwru.server.ApplicationKt.module ]
    }
}

ru.hwru {
    upload {
        dir = ./upload
        dir = ${?UPLOAD_DIR}
    }
}
